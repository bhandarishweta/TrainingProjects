package com.example.consumer.domain;
@Entity
public class Notification {

	@Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String item;
    private Double amount;
	@Override
	public String toString() {
		return "FoodOrder [id=" + id + ", item=" + item + ", amount=" + amount + "]";
	}

}
